// Define level size in px
const int LEVEL_WIDTH = 1280;
const int LEVEL_HEIGHT = 960;

// Define screen size in px
const int SCREEN_WIDTH = 640;
const int SCREEN_HEIGHT = 480;

// Define number of enemies present in game
const int NUMBER_OF_ENEMIES = 5;

// Define entity size in px
const int ENTITY_SIZE = 20;

// Define number of bullets per enemy
const int NUMBER_OF_BULLETS = 4;

// Define overall number of bullets
const int NUMBER_OF_ALL_BULLETS = NUMBER_OF_ENEMIES * NUMBER_OF_BULLETS;

// Define bullet velocity
const int BULLET_VELOCITY = 15;

// Define player health
const int PLAYER_HEALTH = 3;

// Define amount of invincibility frames
const int INVINCIBILITY_FRAMES = 50;

// Define menu label size in px
const int LABEL_WIDTH = 250;
const int LABEL_HEIGHT = 60;